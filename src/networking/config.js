const baseURL = 'https://www.breakingbadapi.com';

export const pointUrl = baseURL;

export const parentUrl = pointUrl + '/api/';

export const headers = {
  'Content-Type': 'application/json',
  'Cache-Control': 'no-cache',
};
